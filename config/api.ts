/**
 * API configuration.
 * Change baseUrl to point at a staging/dev server when needed.
 */
export const api = {
  baseUrl: 'https://branzia.app/api/merchant',
  storeBaseUrl: 'https://branzia.app/store',
} as const;
